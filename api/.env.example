# Backend env vars

# Port на който server-ът ще слуша
PORT=3001

# Comma-separated списък на allowed origins за CORS
# В production: https://your-app.vercel.app
# Локално: * (allow all)
CORS_ORIGIN=*
